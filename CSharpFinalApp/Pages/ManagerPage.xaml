<UserControl x:Class="CSharpFinal.Pages.ManagerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="800" MinHeight="650" MinWidth="750">
    <UserControl.Background>
        <LinearGradientBrush>
            <GradientStop Color="#CE9FFC" Offset="0.0" />
            <GradientStop Color="#7367F0" Offset="0.50" />
        </LinearGradientBrush>
    </UserControl.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- Manager Card (Left) -->
        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="30">
            <materialDesign:Card
                materialDesign:UniformCornerRadius="20"
                materialDesign:ElevationAssist.Elevation="Dp16"
                MaxWidth="630" MaxHeight="700" MinHeight="600" MinWidth="400">
                <StackPanel Orientation="Vertical" Margin="20" VerticalAlignment="Stretch">
                    <!-- Logout Button at the top -->
                    <Button Content="Вийти" Height="35" Margin="0 0 0 15"
                            HorizontalAlignment="Left"
                            Click="OnLogoutClick"
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                    
                    <TextBlock Text="Додати завдання" FontSize="22" Padding="0 10 0 10"
                               FontWeight="Bold" Foreground="Azure" HorizontalAlignment="Center"/>
                    <StackPanel Orientation="Vertical" Margin="0 0 0 10">
                        <TextBlock Text="Оберіть працівника:" Foreground="White" Margin="0 5 0 2"/>
                        <ComboBox x:Name="WorkerComboBox" DisplayMemberPath="Name" SelectedValuePath="Id"
                                  Background="Transparent" Foreground="White" Margin="0 0 0 10"/>
                        <TextBlock Text="Опис завдання:" Foreground="White" Margin="0 5 0 2"/>
                        <TextBox x:Name="TaskDescriptionTextBox"
                                 materialDesign:HintAssist.Hint="Опис"
                                 materialDesign:HintAssist.Foreground="White"
                                 Foreground="White"
                                 Background="Transparent"
                                 BorderBrush="White"
                                 Margin="0 0 0 10"/>
                        <TextBlock Text="Дедлайн:" Foreground="White" Margin="0 5 0 2"/>
                        <DatePicker x:Name="DeadlineDatePicker"
                                    SelectedDateFormat="Short"
                                    Background="Transparent"
                                    Foreground="White"
                                    BorderBrush="White"
                                    Margin="0 0 0 10"/>
                        <Button Content="Додати завдання" Margin="0 10 0 0" Height="35"
                                Click="OnAddTaskClick"
                                Style="{StaticResource MaterialDesignRaisedButton}" />
                    </StackPanel>
                    <Separator Margin="0 10 0 10"/>
                    <TextBlock Text="Список завдань" FontSize="18" FontWeight="Bold" Foreground="Azure" Margin="0 0 0 5"/>
                    <Border BorderBrush="White" BorderThickness="2" CornerRadius="10" Background="#333446" Margin="0 0 0 10">
                        <DataGrid x:Name="TasksDataGrid" Height="180"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  Background="Transparent"
                                  Foreground="White"
                                  BorderThickness="0"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Опис" Binding="{Binding Description}" Width="*"/>
                                <DataGridTextColumn Header="Працівник" Binding="{Binding WorkerName}" Width="*"/>
                                <DataGridTextColumn Header="Дедлайн" Binding="{Binding Deadline, StringFormat=d}" Width="*"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                    <Button Content="Оновити завдання" Height="40" Margin="0 10 0 0"
                            Click="OnUpdateTasksClick"
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
        <!-- Image (Right) -->
        <Image Grid.Column="1"
               Source="/Assets/Images/fon.png"
               Stretch="UniformToFill"
               VerticalAlignment="Stretch"
               HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>